# FrameJoin Studio 0.19

1. 序列帧输出新增“按目标码流输出”开关。
2. 默认不勾选时，继续按所选 H.264、H.265 或 FFV1 真无损输出。
3. 勾选后可填写 1–1000 Mbps，H.264/H.265 按目标码流与峰值限制输出。
4. FFV1 保持数学无损，不允许启用目标码流模式。
5. 目标码流模式明确标注为有损压缩，不再误称无损。
6. 输出摘要、兼容性说明、工程保存与默认文件名均会记录并显示码流模式。
7. 视频原码极速拼接、序列帧输入帧率、静默预览与上一帧/下一帧继续保留。
